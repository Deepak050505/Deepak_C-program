#include<stdio.h>
union book{
int num;
char name[50];
float price;
};
void main()
{
  union book dr;
  dr.num=1;
  dr.price=1000;
  strcpy(dr.name,"Jhon Cena");
  printf("Book number is %d\n",dr.num);
  printf("Book name is %s\n",dr.name);
  printf("Book price is %f\n",dr.price);

}
